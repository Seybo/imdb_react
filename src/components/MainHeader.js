import React from 'react'
import { Card, CardTitle } from 'material-ui/Card';

const MainHeader = () => (
  <Card className="container">
    <CardTitle title="Movie DB" subtitle="This is a learning app which resembles a Movie searching \ rating service" />
  </Card>
)

export default MainHeader;
